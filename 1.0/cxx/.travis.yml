sudo: required

language: ruby

services:
  - docker

before_install:
  - docker build -t andyaugustin/docker_ubuntu14.04_grpc .
  - docker run -d -p 127.0.0.1:80 4567 andyaugustin/docker_ubuntu14.04_grpc 
  - docker ps -a

script:
  - bundle exec rake test

